// 1. Create a variable 'multiply' that multiplies 'num1' and 'num2' and the result is 62.
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // This will be 62

// 2. Create a variable 'random' that generates a random integer greater than 0.
const random = Math.floor(Math.random() * 100) + 1; // Random integer between 1 and 100

// 3. Create a variable 'mod' that calculates the remainder of dividing 'num3' by 'num4' and the remainder is 4.
const num3 = 10;
const num4 = 6;
const mod = num3 % num4; // This will give the remainder 4

// 4. Create a variable 'max' that finds the highest number in a set, and the value returned is 20.
const max = Math.max(1, 5, 10, 20, 15); // The maximum value in this set is 20
